import { centered } from "@fwoosh/decorator-centered";
import { Decorator } from "@fwoosh/react";
import { viewport } from "@fwoosh/tool-viewport/dist/decorator";

export const decorators: Decorator[] = [viewport, centered];
